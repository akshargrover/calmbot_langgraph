import pandas as pd
import datetime

def forecast_mood(state):
    # Simulate forecast (real: use moving average, Prophet, etc.)
    forecast = "Likely increase in anxiety during evenings"
    
    return {**state, "forecast": forecast}
